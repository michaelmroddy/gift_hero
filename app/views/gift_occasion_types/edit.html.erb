<!-- Validation failure messages -->
<% if @gift_occasion_type.errors.any? %>
  <% @gift_occasion_type.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Gift Occasion Type #<%= @gift_occasion_type.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_gift_occasion_type/<%= @gift_occasion_type.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <button class="btn btn-warning">
        Update Gift Occasion Type
      </button>
      or
      <a href="/gift_occasion_types/<%= @gift_occasion_type.id %>">Cancel</a>
    </form>
  </div>
</div>
